#!/bin/sh
# Exit 0 if $KIT_ROLE is healthy and ready to take over. Without this file,
# "its container is running" is all that's checked.
"$KIT" role-exec "$KIT_ROLE" "curl -fsS http://127.0.0.1:4000/health" >/dev/null
