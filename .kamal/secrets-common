# Kamal's secrets for every destination (Kamal reads .kamal/secrets only
# without one), read on the machine that deploys. Only the registry
# token: the app's secrets stay on the server, decrypted there
# (deploy/server-sync.sh) and handed to the containers as --env-file.
KAMAL_REGISTRY_PASSWORD=$(deploy/kamal/registry-password)
