#!/bin/sh
# Is $KIT_ROLE the collector collecting? Its own status port says (what
# deploy.sh's leader() asked before).
"$KIT" role-exec "$KIT_ROLE" "curl -fsS http://127.0.0.1:4101/status" | grep -q '"role":"leader"'
