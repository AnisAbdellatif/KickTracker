# For the machine that deploys (deploy/kamal/registry-password), not the
# app: encrypt to deployer.sops.env like the others (secrets/README.md).
#
# A GitHub token (classic) with read:packages only: Kamal logs the server
# in to GHCR with it at each deploy, to pull the images CI built.
GHCR_READ_TOKEN=
