# Loaded at boot with the definitions made by make-prod-definitions.sh.
load_definitions = /etc/rabbitmq/definitions.json
# Refuse publishes before the disk is full; the receivers spool meanwhile
# and forward once RabbitMQ accepts again (project.md §8.4).
disk_free_limit.absolute = 2GB
