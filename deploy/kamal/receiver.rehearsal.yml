# The deploy rehearsal (deploy/rehearsal/README.md): what differs from
# production for Kamal, merged over receiver.yml by `-d rehearsal`: the fake
# Kick runs on the host.
servers:
  receiver_1:
    options:
      add-host: host.docker.internal:host-gateway
  receiver_2:
    options:
      add-host: host.docker.internal:host-gateway
